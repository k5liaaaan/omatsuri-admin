# サーバーデプロイ用環境変数ファイル
# 使用方法:
# 1. このファイルを .env にコピー: cp .env_server .env
# 2. 各設定値を実際の値に変更してください

NODE_ENV=production
PORT=3001

# データベース接続
# 形式: postgresql://[ユーザー名]:[パスワード]@[ホスト]:[ポート]/[データベース名]
# 例: postgresql://omatsuri_user:password123@localhost:5432/omatsuri_db
DATABASE_URL=postgresql://omatsuri_user:CHANGE_ME_PASSWORD@localhost:5432/omatsuri_db

# JWT設定
# 強力なランダム文字列に変更してください（例: openssl rand -hex 32 で生成）
JWT_SECRET=CHANGE_ME_JWT_SECRET
JWT_EXPIRES_IN=365d

# フロントエンドURL（CORS用）
# 実際のドメインに変更してください
FRONTEND_URL=https://CHANGE_ME_FRONTEND_DOMAIN.com
FRONTEND_BASE_URL=https://CHANGE_ME_FRONTEND_DOMAIN.com

# メール設定（必須）
MAIL_HOST=smtp.CHANGE_ME_DOMAIN.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=CHANGE_ME_SMTP_USER
MAIL_PASS=CHANGE_ME_SMTP_PASSWORD
MAIL_FROM=no-reply@CHANGE_ME_DOMAIN.com

# メール設定（任意）
# デフォルト値を使用する場合はコメントアウトのままでOK
# MAIL_REPLY_TO=support@CHANGE_ME_DOMAIN.com
# MAIL_SUBJECT_REGISTRATION=【おまつり管理】メールアドレスの確認について
# MAIL_BODY_REGISTRATION=カスタム本文（{{verificationUrl}} と {{expiresAt}} を含める）
# MAIL_SUBJECT_EMAIL_CHANGE=【おまつり管理】新しいメールアドレスの確認
# MAIL_BODY_EMAIL_CHANGE=カスタム本文（{{verificationUrl}} と {{expiresAt}} を含める）

